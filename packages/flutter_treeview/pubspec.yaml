name: flutter_treeview
description: A tree widget for Flutter that can be used to display nested, hierarchical data.
version: 0.0.1
author:
homepage:

publish_to: 'none'

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
