/// Types of properties of a widget
enum PropertyType {
  icon,
  double,
  integer,
  string,
  mainAxisAlignment,
  crossAxisAlignment,
  widget,
  color,
  alignment,
  boxFit,
  boolean,
  scrollPhysics,
  axis,
  fontStyle
}

enum GroupType {
  child,
  children,
  appbar,
  body,
  floatingActionButton,
}
